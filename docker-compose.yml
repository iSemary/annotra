services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: annotra
      POSTGRES_PASSWORD: annotra
      POSTGRES_DB: annotra
    ports:
      - "15432:5432"
    volumes:
      - annotra_pg_data:/var/lib/postgresql/data

volumes:
  annotra_pg_data:
